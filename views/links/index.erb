<table>
  <tr>
    <th>Title</th>
    <th>Hostname</th>
    <th>Description</th>
    <th></th>
  </tr>

  <% links.each do |link| %>
    <tr id="links-<%= link.id %>">
      <td>
        <a href="<%= link.url %>">
          <%= link.title %>
        </a>
      </td>

      <td>
        <%= link.hostname %>
      </td>

      <td>
        <%= link.description %>
      </td>

      <td>
        <form class="delete-link right" method="POST" action="/links/<%= link.id %>">
          <input type="hidden" name="_method" value="DELETE">
          <input type="hidden" name="name" value="<%= link.title %>">
          <input class="button tiny" type="submit" value="Delete">
        </form>
      </td>
    </tr>
  <% end %>
</table>
